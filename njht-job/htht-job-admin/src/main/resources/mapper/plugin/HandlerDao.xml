<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.htht.job.admin.plugin.dao.HandlerDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.htht.job.admin.plugin.entity.HandlerEntity" id="handlerMap">
        <result property="id" column="id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="version" column="version"/>
        <result property="modelIdentify" column="model_identify"/>
        <result property="modelName" column="model_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="type" column="type"/>
        <result property="templateId" column="template_id"/>
        <result property="groupId" column="group_id"/>
    </resultMap>
    <select id="selectRegisterValueById" resultType="java.lang.String">
        SELECT a.address_list FROM `xxl_job_group` a
        LEFT JOIN htht_cluster_schedule_handler b
        ON a.id = b.group_id
        WHERE b.id = #{handlerId}
    </select>


</mapper>