<?xml version="1.0" encoding="UTF-8"?>
		<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.njht.webyun.management.order.dao.ShoppingCartInfoDao">

	<resultMap type="com.njht.webyun.management.order.entity.ShoppingcartInfo" id="BaseResultMap">
		<result property="id" column="id"/>
		<result property="createTime" column="create_time"/>
		<result property="userId" column="user_id"/>
	</resultMap>
	<update id="updateSatelliteShopCartInfo">
		UPDATE
			`shoppingcart_list`
		SET
			`data_type` = #{dataType}
		WHERE
			`cart_id` = #{cartId} and`data_id` = #{dataId}

	</update>

	<select id="selectShoppingcart" resultMap="BaseResultMap" parameterType="java.lang.String">
		select id, create_time, update_time, user_id
		from shoppingcart_info
		<where>
			<if test="userId != null and userId != ''">
				user_id = #{userId}
			</if>
		</where>
	</select>
    <select id="getShoppingInfoByDataId" resultType="java.lang.String">
		SELECT data_type FROM `shoppingcart_list`
		where data_id = #{dataId} and cart_id = #{cartId}
	</select>

    <insert id="insertCartInfo" parameterType="java.lang.String">
		<selectKey keyProperty="id" resultType="string" order="BEFORE">
			select replace(uuid(),'-','') from dual
		</selectKey>
		insert into shoppingcart_info
			(id,create_time,user_id)
			<trim prefix="values (" suffix=")" suffixOverrides="," >
			#{id},now()<if test="userId != null and userId != ''">,#{userId}</if>
			</trim>
		ON DUPLICATE KEY
		<trim prefix="update" suffixOverrides=",">
		create_time=now(),
			<if test="userId != null and userId != ''">user_id=#{userId},</if>
		</trim>
	</insert>
</mapper>